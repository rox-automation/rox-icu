{
  "initializeCommand": "mkdir -p /var/tmp/container-extensions-icu", // executed on HOST system
  "build": {
    "dockerfile": "Dockerfile",
    "args": {
      "USER_UID": "${localEnv:USER_UID}",
      "USER_GID": "${localEnv:USER_GID}"
    }
  },
  "runArgs": [
    "--network",
    "host",
    "--privileged"
  ],
  "mounts": [
    "source=/var/tmp/container-extensions-icu,target=/home/dev/.vscode-server/extensions,type=bind,consistency=cached"
  ],
  "postCreateCommand": ".devcontainer/init_container.sh",
  // set env variables
  "containerEnv": {
    "PYTHONPATH": "${containerWorkspaceFolder}/embedded_lib",
    "ICU_INTERFACE": "vcan0"
  },
  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "shardulm94.trailing-spaces",
        "mhutchie.git-graph",
        "doi.fileheadercomment",
        "donjayamanne.githistory",
        "ms-python.mypy-type-checker",
        "tamasfe.even-better-toml",
        "GitHub.copilot",
        "ms-python.pylint",
        "ms-python.black-formatter"
      ]
    }
  }
}
